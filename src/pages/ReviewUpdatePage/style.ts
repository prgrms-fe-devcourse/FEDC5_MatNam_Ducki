import styled from '@emotion/styled';

import { theme } from '@/styles/Theme';

export const NotUpdateInput = styled.span`
  color: ${theme.colors.gray};
  border: none;
  border-bottom: 1px solid #f3f2f2;
  padding-bottom: 1rem;
`;
