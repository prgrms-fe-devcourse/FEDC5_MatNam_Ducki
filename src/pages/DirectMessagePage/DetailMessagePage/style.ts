import styled from '@emotion/styled';

export const DetailMessagePageWrapper = styled.ul`
  width: 100%;
  position: relative;
  top: 8.5rem;
  height: calc(100% - 16rem);
  display: flex;
  flex-direction: column;
  gap: 1.5rem;
`;

export const UserProfileWrapper = styled.div`
  position: fixed;
  top: 2rem;
`;
